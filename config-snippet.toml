# Add this to your .codex/config.toml (repo-local or ~/.codex/config.toml).
# It registers the reviewer subagent and caps how many agents can run.

[agents]
max_threads = 6      # max concurrent subagents

[agents.reviewer]
description = "Reviews the final diff, read-only."
config_file = "./agents/reviewer.toml"
